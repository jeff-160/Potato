#include <vector>

namespace Potato{
    class Character{
        public:
            std::string Name;
            int X, Y;
            int Width, Height;
            Uint32 Opacity;
        
        Character(std::string Name, int X, int Y, int Width, int Height){

        }
    };
}